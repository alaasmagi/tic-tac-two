@page
@model WebApp.Pages_Configurations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfigurationEntity[0].GameConfigName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigurationEntity[0].SerializedJsonString)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConfigurationEntity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GameConfigName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerializedJsonString)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
